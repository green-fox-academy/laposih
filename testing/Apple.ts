// Create a class with one method (eg. getApple()) that returns a string (eg. 'apple')
// Create a test for that:

'use strict';

export class Apple {
    getApple(): string {
        return 'apple';
    }
}